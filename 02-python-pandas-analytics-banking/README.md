Culminating Project for ITM 618 where I trained a model using a test set of data and evaluated using ROC curves.
- Observations between regression model versus decision trees is that regression models are more "reliable" when testing actual data-- rather than just the test data.
